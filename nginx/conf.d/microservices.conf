server {
    listen 8001;
    server_name localhost;
    access_log /var/log/nginx/servico1.log main;

    location / {
        root /usr/share/nginx/microservices;
        index servico1.html;
    }
}

server {
    listen 8002;
    server_name localhost;
    access_log /var/log/nginx/servico2.log main;

    location / {
        root /usr/share/nginx/microservices;
        index servico2.html;
    }
}
